# Generated by Django 4.2.7 on 2024-01-24 08:36

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("experiments", "0058_survey_confirmation_text"),
    ]

    operations = [
        migrations.AddField(
            model_name="experiment",
            name="max_token_limit",
            field=models.PositiveIntegerField(
                default=8192,
                help_text="When the message history for a session exceeds this limit (in tokens), it will be compressed. If 0, compression will be disabled which may result in errors or high LLM costs.",
            ),
        ),
        migrations.AlterField(
            model_name="survey",
            name="url",
            field=models.URLField(
                help_text="Use the {participant_id}, {session_id} and {experiment_id} variables if you want to include the participant, session and experiment session ids in the url.",
                max_length=500,
            ),
        ),
    ]
