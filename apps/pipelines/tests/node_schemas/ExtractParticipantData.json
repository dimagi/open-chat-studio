{
  "description": "Extract structured data and saves it as participant data",
  "properties": {
    "tag": {
      "default": "",
      "description": "The tag that the output message should be tagged with",
      "title": "Message Tag",
      "type": "string"
    },
    "llm_provider_id": {
      "description": "The configured LLM service provider this node calls, e.g. the team's OpenAI account.",
      "title": "LLM Model",
      "type": "integer",
      "ui:widget": "llm_provider_model"
    },
    "llm_provider_model_id": {
      "description": "The model to call, e.g. gpt-4o. Must belong to the same provider type as `llm_provider_id` -- an OpenAI provider cannot run an Anthropic model.",
      "title": "Llm Provider Model Id",
      "type": "integer",
      "ui:widget": "none"
    },
    "llm_model_parameters": {
      "additionalProperties": true,
      "description": "Generation settings passed to the model, such as `temperature`. Which keys are accepted depends on the model; leave empty to use its defaults.",
      "title": "Llm Model Parameters",
      "type": "object",
      "ui:widget": "none"
    },
    "name": {
      "description": "Identifies the node within its pipeline. Another node reaches this one's result through `temp_state.outputs.<name>`, so renaming it breaks any template that does.",
      "title": "Node Name",
      "type": "string",
      "ui:widget": "node_name"
    },
    "data_schema": {
      "default": "{\"name\": \"the name of the user\"}",
      "description": "A JSON object structure where the key is the name of the field and the value the description",
      "title": "Data Schema",
      "type": "string",
      "ui:widget": "expandable_text"
    },
    "key_name": {
      "default": "",
      "description": "Nests the extracted object under this key in the participant's data instead of merging it in at the top level. Leave empty to merge.",
      "title": "Key Name",
      "type": "string"
    }
  },
  "required": [
    "llm_provider_id",
    "llm_provider_model_id",
    "name"
  ],
  "title": "ExtractParticipantData",
  "type": "object",
  "ui:can_add": true,
  "ui:can_delete": true,
  "ui:deprecated": false,
  "ui:documentation_link": "/concepts/pipelines/nodes/#update-participant-data-node",
  "ui:flow_node_type": "pipelineNode",
  "ui:icon": "fa-solid fa-user-pen",
  "ui:label": "Update Participant Data"
}
