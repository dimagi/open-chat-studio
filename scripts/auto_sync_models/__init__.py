"""Daily reconciliation of OCS's model catalogue and pricing seed against LiteLLM.

Run it as ``python3 -m scripts.auto_sync_models.run`` from the repo root; see
``run`` for what one pass produces. Stdlib only, so CI needs no dependency
install.
"""
